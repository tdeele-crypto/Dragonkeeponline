# Trimmed dependency set for self-hosting Dragon Keeper on your own server.
# The full backend/requirements.txt contains Emergent-platform-only packages
# (emergentintegrations) and build-heavy extras (jq, pandas, numpy) that are
# NOT needed to run the app and would break `pip install` on a plain VPS.
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pymongo==4.6.3
motor==3.3.1
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
python-multipart>=0.0.9
tzdata>=2024.2
